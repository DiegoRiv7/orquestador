version: "3.8"

services:
  # ─── Panel estático servido por Nginx ────────────────────────
  panel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-command-bridge-panel
    restart: unless-stopped
    ports:
      - "8080:80"
